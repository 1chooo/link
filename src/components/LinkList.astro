---
import { links } from '../data/links'
import LinkButton from './LinkButton.astro'
import styles from './LinkList.module.css'
---

<nav class={styles.links} aria-label="Social links">
  {links.map((link) => <LinkButton link={link} />)}
</nav>
